import React from 'react';

const Wrapper = () => {
    const accessToken = localStorage.getItem("accessToken");
    return (
        <div>
            Wrapper
        </div>
    );
};

export default Wrapper;